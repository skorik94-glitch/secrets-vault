# Dependencies (the scanner itself has none, but tooling might)
node_modules/

# Scan output — CONTAINS A MAP OF WHERE YOUR SECRETS LIVE. Never commit.
inventory-report*.json
*.report.json
.hush/

# Local secrets / Infisical link (added in later stages)
.env
.env.*
!.env.example
infisical.json

# macOS
.DS_Store

# Internal strategy / roadmap (not for the public repo)
marketing/
ExecPlan.md
.secrets-vault/
.secrets-inventory/
